# DMS 系統使用者手冊

## 🎯 快速開始

### 第一步：啟動資料庫

**為什麼需要這一步？**
- 系統需要 SQL Server 資料庫來儲存資料
- 使用 Docker 容器可以快速啟動，無需安裝 SQL Server

**如何執行：**
```bash
cd /Users/kaichanghuang/Documents/Phoenix\ Code/DmsSystem
docker-compose up -d
```

**檢查是否成功：**
```bash
docker ps | grep sqlserver
```
應該看到 `dms-sqlserver` 容器正在運行

**等待時間：** 約 30-60 秒讓 SQL Server 完全啟動

---

### 第二步：載入測試資料

**為什麼需要這一步？**
- 資料庫是空的，需要一些測試資料才能看到效果
- 測試資料包含台積電、鴻海、聯發科等公司的股東會資訊

**如何執行：**
```bash
# 複製測試資料腳本到容器
docker cp scripts/seed-test-data.sql dms-sqlserver:/tmp/seed-test-data.sql

# 執行腳本
docker exec -i dms-sqlserver /opt/mssql-tools/bin/sqlcmd \
  -S localhost \
  -U sa \
  -P 'DmsSystem@2024' \
  -d DMS \
  -i /tmp/seed-test-data.sql
```

**檢查是否成功：**
```bash
docker exec -it dms-sqlserver /opt/mssql-tools/bin/sqlcmd \
  -S localhost \
  -U sa \
  -P 'DmsSystem@2024' \
  -d DMS \
  -Q "SELECT COUNT(*) FROM RIS.SHMT_SOURCE1"
```
應該看到數字 3（3 筆測試資料）

---

### 第三步：啟動後端 API

**為什麼需要這一步？**
- 後端 API 提供資料處理和查詢功能
- 前端需要透過 API 來存取資料

**如何執行：**
```bash
cd DmsSystem.Api
dotnet run
```

**應該看到：**
```
[10:33:02 INF] 應用程式啟動中...
[10:33:02 INF] 應用程式啟動完成
[10:33:02 INF] Now listening on: http://localhost:5137
```

**檢查是否成功：**
1. 打開瀏覽器訪問：http://localhost:5137/swagger
2. 應該看到 Swagger API 文件頁面

**保持這個終端視窗開啟**，API 需要持續運行

---

### 第四步：啟動前端（新終端視窗）

**為什麼需要這一步？**
- 前端提供使用者介面
- 讓使用者可以上傳檔案和查看資料

**如何執行：**
```bash
# 開啟新的終端視窗
cd /Users/kaichanghuang/Documents/Phoenix\ Code/DmsSystem/react-client

# 首次執行需要安裝依賴
npm install

# 啟動開發伺服器
npm run dev
```

**應該看到：**
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

**檢查是否成功：**
1. 打開瀏覽器訪問：http://localhost:5173
2. 應該看到 DMS 系統的首頁

---

## 📊 資料流程說明

### 整體架構流程

```
┌─────────────┐
│  使用者      │
│  (瀏覽器)    │
└──────┬──────┘
       │
       │ HTTP 請求
       ▼
┌─────────────────┐
│  React 前端      │
│  (Port 5173)     │
│  - 檔案上傳介面   │
│  - 資料檢視介面   │
└──────┬──────────┘
       │
       │ RESTful API
       │ (http://localhost:5137)
       ▼
┌─────────────────┐
│  .NET API       │
│  (Port 5137)     │
│  - 驗證輸入      │
│  - 處理請求      │
└──────┬──────────┘
       │
       │ 業務邏輯
       ▼
┌─────────────────┐
│  Application    │
│  Service 層      │
│  - 檔案解析      │
│  - 資料處理      │
└──────┬──────────┘
       │
       │ 資料存取
       ▼
┌─────────────────┐
│  Infrastructure │
│  Repository     │
└──────┬──────────┘
       │
       │ SQL 查詢
       ▼
┌─────────────────┐
│  SQL Server     │
│  (Port 1433)    │
│  - 儲存資料      │
└─────────────────┘
```

### 檔案上傳流程（詳細）

#### 1. 使用者在網頁選擇檔案
```
使用者操作：點擊「選擇檔案」按鈕，選擇 .xlsx 或 .csv 檔案
前端處理：FileUpload 元件接收檔案
```

#### 2. 前端發送檔案到 API
```javascript
// react-client/src/components/FileUpload.tsx
const result = await apiClient.uploadFile(endpoint, file);
```
- 使用 `FormData` 包裝檔案
- 發送 POST 請求到 `/api/ShareholderMeetings/upload-shmtsource1`

#### 3. API 驗證檔案
```csharp
// DmsSystem.Api/Controllers/ShareholderMeetingsController.cs
var validationResult = await _fileValidator.ValidateAsync(file);
```
- 檢查檔案是否為空
- 檢查檔案大小（不能超過 10MB）
- 檢查檔案格式（只接受 .xlsx 和 .csv）

#### 4. API 呼叫 Service 處理
```csharp
// DmsSystem.Api/Controllers/ShareholderMeetingsController.cs
var result = await _detailService.ProcessUploadAsync(stream, file.FileName);
```
- 將檔案串流傳給 Service 層

#### 5. Service 使用檔案解析器
```csharp
// DmsSystem.Application/Services/ShareholderMeetingDetailService.cs
var entitiesToInsert = await _fileParser.ParseAsync(fileStream, fileName);
```
- 根據檔案類型（.xlsx 或 .csv）選擇對應的解析器
- 解析器讀取檔案內容，轉換為 `ShmtSource1` 實體列表

#### 6. 檔案解析器實作
```csharp
// DmsSystem.Infrastructure/FileParsing/ShmtSource1FileParser.cs
// 如果是 Excel：使用 NPOI 讀取
// 如果是 CSV：使用 CsvHelper 讀取
```
- Excel：使用 NPOI 讀取工作表，逐行解析
- CSV：使用 CsvHelper 讀取，根據欄位順序或名稱對應

#### 7. Service 套用業務邏輯
```csharp
// 在解析器中自動套用
record.AcDate = DateTime.Now.ToString("yyyy-MM-dd HH:mm:ss");
record.EmpNo = "A00994";
record.Status = "Y";
```
- 自動填入處理日期、員工編號等欄位

#### 8. Service 儲存到資料庫
```csharp
// DmsSystem.Application/Services/ShareholderMeetingDetailService.cs
await _repository.AddRangeAsync(entitiesToInsert);
```
- 呼叫 Repository 的批次新增方法

#### 9. Repository 執行資料庫操作
```csharp
// DmsSystem.Infrastructure/Persistence/Repositories/ShmtSource1Repository.cs
await _context.ShmtSource1s.AddRangeAsync(entities);
await _context.SaveChangesAsync();
```
- 使用 Entity Framework Core 批次新增
- 執行 SQL INSERT 語句

#### 10. 回傳結果給前端
```csharp
return (true, $"成功載入 {entitiesToInsert.Count} 筆資料", entitiesToInsert.Count);
```
- 前端收到成功訊息和新增筆數
- 顯示成功通知給使用者

### 資料查詢流程（詳細）

#### 1. 使用者點擊「資料檢視」
```
使用者操作：在網頁上點擊「資料檢視」標籤
前端處理：DataView 元件載入
```

#### 2. 前端發送查詢請求
```javascript
// react-client/src/pages/DataView.tsx
const response = await apiClient.request('/api/DataView/shareholder-meetings');
```
- 發送 GET 請求到 `/api/DataView/shareholder-meetings`

#### 3. API Controller 接收請求
```csharp
// DmsSystem.Api/Controllers/DataViewController.cs
[HttpGet("shareholder-meetings")]
public async Task<IActionResult> GetShareholderMeetings(...)
```
- 接收查詢參數（分頁資訊）

#### 4. Controller 呼叫 Repository
```csharp
var data = await _shmtSource1Repo.GetAllAsync();
```
- 取得所有資料

#### 5. Repository 查詢資料庫
```csharp
// DmsSystem.Infrastructure/Persistence/Repositories/ShmtSource1Repository.cs
return await Task.FromResult(_context.ShmtSource1s.ToList());
```
- 使用 Entity Framework Core 查詢
- 執行 SQL SELECT 語句

#### 6. Controller 處理分頁
```csharp
var pagedData = data.Skip((page - 1) * pageSize).Take(pageSize).ToList();
```
- 根據分頁參數篩選資料

#### 7. 回傳 JSON 資料
```csharp
return Ok(new {
    data = pagedData,
    total,
    page,
    pageSize,
    totalPages = ...
});
```

#### 8. 前端顯示資料
```javascript
// react-client/src/pages/DataView.tsx
setMeetings(response.data || []);
```
- 將資料設定到 state
- React 自動重新渲染表格
- 使用者看到資料列表

## 🔄 資料串接說明

### 資料如何從資料庫到網頁

```
SQL Server 資料庫
    │
    │ Entity Framework Core 映射
    ▼
Domain Entities (ShmtSource1, ShmtSource4, etc.)
    │
    │ Repository 查詢
    ▼
Application Service
    │
    │ Controller 處理
    ▼
API Response (JSON)
    │
    │ HTTP 傳輸
    ▼
React Frontend
    │
    │ State 管理
    ▼
UI 顯示 (表格、列表)
```

### 資料格式轉換

1. **資料庫 → Entity**
   - SQL Server 的資料表欄位
   - 轉換為 C# 類別的屬性
   - 例如：`STK_CD` → `StkCd`

2. **Entity → JSON**
   - C# 物件序列化為 JSON
   - 例如：`{ "stkCd": "2330", "stkName": "台積電" }`

3. **JSON → React State**
   - JSON 解析為 JavaScript 物件
   - 儲存在 React 的 state 中

4. **State → UI**
   - React 元件讀取 state
   - 渲染為 HTML 表格

## 🎓 為什麼要這樣設計？

### 1. 分層架構的好處

**問題：** 如果所有程式碼混在一起會怎樣？
- 難以測試
- 難以維護
- 難以擴展

**解決：** 分層架構
- 每層職責清楚
- 可以獨立測試
- 可以獨立替換

### 2. 前後端分離的好處

**問題：** 如果前端和後端綁在一起會怎樣？
- 只能有一種前端介面
- 無法支援網頁、手機 App 等多種平台

**解決：** API 設計
- 後端提供 API
- 任何前端都可以使用
- 可以同時支援網頁、手機 App

### 3. 介面導向的好處

**問題：** 如果直接使用具體實作會怎樣？
- 換資料庫要改很多地方
- 換檔案解析器要改很多地方

**解決：** 使用介面
- 定義 `IFileParser` 介面
- 可以實作 Excel 解析器、CSV 解析器
- 換解析器只需換實作，不用改業務邏輯

## 📝 執行檢查清單

### 啟動前檢查
- [ ] Docker Desktop 正在運行
- [ ] 已安裝 .NET 8 SDK
- [ ] 已安裝 Node.js 和 npm

### 啟動步驟
- [ ] 步驟 1：啟動資料庫容器
- [ ] 步驟 2：載入測試資料
- [ ] 步驟 3：啟動後端 API
- [ ] 步驟 4：啟動前端

### 驗證步驟
- [ ] 可以訪問 Swagger：http://localhost:5137/swagger
- [ ] 可以訪問前端：http://localhost:5173
- [ ] 前端可以顯示資料
- [ ] 可以上傳檔案

## 🐛 常見問題

### Q: API 啟動失敗，顯示資料庫連接錯誤
**A:** 
1. 確認 SQL Server 容器正在運行：`docker ps`
2. 等待 30-60 秒讓 SQL Server 完全啟動
3. 檢查 `appsettings.Development.json` 中的連接字串

### Q: 前端無法連接到 API
**A:**
1. 確認 API 正在運行（檢查終端視窗）
2. 確認 API URL 正確：http://localhost:5137
3. 檢查瀏覽器控制台的錯誤訊息

### Q: 資料顯示為空
**A:**
1. 確認已載入測試資料（執行步驟 2）
2. 檢查資料庫中是否有資料
3. 檢查 API 是否正常回傳資料（使用 Swagger 測試）

## 📚 下一步

1. **上傳檔案測試**：嘗試上傳 Excel 或 CSV 檔案
2. **查看資料**：在「資料檢視」頁面查看已上傳的資料
3. **閱讀架構文件**：了解系統設計原理
4. **執行測試**：執行單元測試和整合測試

---

**現在您可以開始使用系統了！** 🎉

如有任何問題，請參考其他文件或檢查執行狀態報告。

